version: '3'
services:
  web:
    container_name: user-center-client
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "443:443"
    env_file:
      - .env
    volumes:
      - './ssl:/etc/nginx/cert'
